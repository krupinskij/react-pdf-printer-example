import { useTranslation } from 'react-i18next';

import * as Styled from './Footer.styles';

const Footer = () => {
  return (
    <Styled.Wrapper>
      <Styled.Pagination />
    </Styled.Wrapper>
  );
};

export default Footer;
